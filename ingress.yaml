apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jenkins-ingress-github
  namespace: jenkins-prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: "nginx"
  tls:
    - secretName: jenkins-synergy-cert
  rules:
  - host: jenkins-gh.a.incd.pt
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jenkins-operator-http-jenkins-synergy
            port:
              number: 8080